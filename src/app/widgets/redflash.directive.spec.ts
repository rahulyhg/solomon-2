import { RedflashDirective } from './redflash.directive';

describe('RedflashDirective', () => {
  it('should create an instance', () => {
    const directive = new RedflashDirective();
    expect(directive).toBeTruthy();
  });
});
