import { RedtoggleDirective } from './redtoggle.directive';

describe('RedtoggleDirective', () => {
  it('should create an instance', () => {
    const directive = new RedtoggleDirective();
    expect(directive).toBeTruthy();
  });
});
